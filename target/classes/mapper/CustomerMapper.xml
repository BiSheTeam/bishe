<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="team.bishe.wms.mapper.CustomerMapper">
    <resultMap id="customer" type="team.bishe.wms.bean.Customer">
        <id property="id" column="CUSTOMER_ID"/>
        <result property="name" column="CUSTOMER_NAME"/>
        <result property="telephone" column="CUSTOMER_TEL"/>
        <result property="email" column="CUSTOMER_EMAIL"/>
        <result property="address" column="CUSTOMER_ADDRESS"/>
    </resultMap>

    <select id="selectAll" resultMap="customer">
        select * from wms_customer
    </select>

    <select id="selectById" resultMap="customer">
        select * from wms_customer where customer_id = #{id}
    </select>

    <select id="selectByName" resultMap="customer">
        select * from wms_customer where customer_name = #{name}
    </select>

    <insert id="insert">
        insert into wms_customer(customer_id,customer_name,customer_tel,customer_email,customer_address)
        values(#{id},#{name},#{telephone},#{email},#{address})
    </insert>

    <update id="update">
        update wms_customer
        set customer_name = #{name},
            customer_tel = #{telephone},
            customer_email = #{email},
            customer_address = #{address}
        where customer_id =#{id}
    </update>

    <delete id="deleteById">
        delete from wms_customer
        where customer_id = #{id}
    </delete>

    <delete id="deleteByName">
        delete from wms_customer
        where customer_name = #{name}
    </delete>
</mapper>
