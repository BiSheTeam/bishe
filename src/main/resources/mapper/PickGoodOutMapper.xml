<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="team.bishe.wms.mapper.PickGoodOutMapper">
    <!--获取拣选对外返回参数-->
    <select id="pickGood" resultType="team.bishe.wms.pojo.PickGoodOutResp">
        select
            p.g_form_id as gFormId,
            p.g_pos_id as gPosId,
            p.good_nm as goodNm,
            p.good_num as goodNum,
            p.tray_id as TrayId,
            from pick_form p
            where p.pick_mode=#{pickMode}
    </select>

</mapper>